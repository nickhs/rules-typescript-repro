import * as AvatarCell from 'bazel_rules_typescript/src/avatar_cell/avatar_cell';

export const NAME = 'GRID_ROW';

export function create() {
    const thing = AvatarCell.create({ size: 'small' })
    return undefined;
}
